class View {
    gitLink = document.querySelector(
        "main .main-container .info .link .git-link"
    );
    coloring() {
        // let colored = this.gitLink.textContent.split(" ");
        // let coloredArr = colored.map(l => l == "const" ? )
        // console.log(colored);

        console.log("تم الإستسلام بتلوين الكلام باستخدام الجافا سكريبت");
    }
}
export default new View();
